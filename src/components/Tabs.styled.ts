import styled from "styled-components";

export const StyledTabs = styled.div`
  color: black;
  font-weight: 100;
  display: flex;
  flex-grow: 1;
  width: 100%;
  justify-content: space-evenly;
  padding: 1rem;

  p:hover {
    cursor: pointer;
  }
`;
